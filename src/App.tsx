import {ReactElement} from 'react';
import './App.css';

const App: () => ReactElement | null = () => {
    return (
        <div className='App'>
            <h1>Weather App</h1>
        </div>
    );
}

export default App;
